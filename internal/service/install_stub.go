package service

import "errors"

// Filled in by the install step.
func install() error   { return errors.New("не реализовано") }
func uninstall() error { return errors.New("не реализовано") }
func installed()       {}
