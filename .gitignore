# Secrets
.env
!.env.example

# Generated runtime files
xray_config.json
xray_config.json.lock
xray-runner.log
last_server.json
error.txt

# Build output
myxray.exe
*.exe
/xray-runner

# Test coverage output
*.out
cov
out

# Xray dependencies (download separately)
xray.exe
geoip.dat
geosite.dat
wintun.dll

# Distribution bundles
xray_linux/
xray_windows/
linux_deploy/
windows_deploy/
*.zip

# Go build cache
go.work
go.work.sum

# OS files
Thumbs.db
.DS_Store

IMPLEMENTATION.md
PLAN.md
AGENTS.md
PROJECT.md
.opencode
.claude
.superpowers/

# Working specs / audit notes — kept locally, not published
docs/*
# ...except the agent-skills config, which the whole repo needs
!docs/agents/

# Subscription URLs
subscriptions.txt

# Per-machine device identifier (generated on first run)
hwid.txt

# Per-machine split-tunnel process list
apps.txt

# dumped bare links (contain UUIDs/passwords)
keys/

# configs saved from the viewer (contain UUIDs/passwords)
configs/

# Geo databases installed per subscription
geo/

# Assets staged into the single-file build
internal/bundle/assets/
.task/

# Local issue tracker (specs and tickets for the agent skills)
.scratch/
